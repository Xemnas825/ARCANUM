# Database
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=arcanum_db

# Server
PORT=3001
NODE_ENV=development

# Auth (producción: definir JWT_SECRET fuerte)
# JWT_SECRET=tu_clave_secreta_muy_larga

# Admin por defecto (solo si se usa el seed de init; opcional)
# ADMIN_DEFAULT_PASSWORD=Admin123!
